Resource(shader, "res://engine/shaders/pbr") : shader;

Shader(resource = @shader);
Uniform()
{
    vec4("diffuse", [ 0, 0, 0, 1 ]);
    vec4("specular", [ 0, 0, 0, 1 ]);
    vec4("emissive", [ 1, 0, 1, 0 ]);
    float("specular_exponent", 32);
    float("specular_factor", 0.4);
};