Resource(shader, "res://engine/shaders/deferred") : shader;

Shader(resource = @shader);
Uniform()
{
    vec4("base_colour", [ 1, 0.6, 0.2, 1 ]);
    vec4("specular_colour", [ 1, 0.6, 0.2, 1 ]);
    
    float("roughness_factor_add", 0.1);
    float("metallic_factor_add", 1.0);
};
