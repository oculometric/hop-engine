Resource(shader, "res://engine/shaders/deferred.glsl") : shader;
Resource(texture, "res://museum/Earth_t.png") : albedo;
Resource(texture, "res://museum/Earth_n.png") : normal;

Shader(resource = @shader);
Texture(binding = "albedo_tex", resource = @albedo, filter = NEAREST);
Texture(binding = "normal_tex", resource = @normal, filter = NEAREST);
Uniform()
{
    vec4("base_colour", [ 1, 1, 1, 1 ]);
    vec4("specular_colour", [ 1, 1, 1, 1 ]);
    
    float("roughness_factor_add", 1.0);
    float("metallic_factor_add", 0.0);
    float("normal_strength", 1.0);
    float("use_triplanar", 1.0);
    float("triplanar_scale", 0.5);
};