Resource(shader, "res://engine/shaders/deferred.glsl") : shader;
Resource(texture, "res://museum/PalmFrond_t.png") : albedo;
Resource(texture, "res://museum/PalmFrond_n.png") : normal;

Shader(resource = @shader);
Culling(mode = NONE);
Texture(binding = "albedo_tex", resource = @albedo, filter = NEAREST);
Texture(binding = "normal_tex", resource = @normal, filter = NEAREST);
Uniform()
{
    vec4("base_colour", [ 1, 1, 1, 1 ]);
    vec4("specular_colour", [ 0.05, 0.3, 0.01, 1 ]);
    
    float("roughness_factor_add", 0.6);
    float("metallic_factor_add", 0.4);
    float("normal_strength", 0.8);
};